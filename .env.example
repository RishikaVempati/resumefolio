# Copy to .env and fill in real values. Never commit .env.
# Variables appear here one slice ahead of when the code reads them.

# Google AI Studio key for Gemini. Unset -> the app uses StubParser. (slice 2)
GEMINI_API_KEY=

# Supabase Postgres connection string. (slice 3)
DATABASE_URL=
